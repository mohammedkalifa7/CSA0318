include <stdio.h>
#define MAX 5
int stack[MAX] = {10, 20, 30}, top = 2;

void pop() {
    if (top == -1)
        printf("Stack Underflow\n");
    else
        printf("Popped element: %d\n", stack[top--]);
}

int main() {
    pop();
    return 0;
}
